# AskMe

### Requirements

`ruby >= 3.1.3`

`rails ~> 7.0.4`

### Installation & using

In `terminal` or `cmd`

- `$ git clone git@github.com:mrjoshua22/askme.git`

- `$ gem install bundler` - if needed

- `$ bundle` 

- `$ rails db:migrate`

- `$ rails s`
